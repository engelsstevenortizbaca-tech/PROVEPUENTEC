# ==============================================================
# Servidor
# ==============================================================
NODE_ENV=development
PORT=3000
API_PREFIX=/api

# ==============================================================
# Base de datos (MySQL 8)
# ==============================================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=marketplace
DB_CONNECTION_LIMIT=10

# ==============================================================
# CORS (orígenes separados por coma; * = permitir todos)
# ==============================================================
CORS_ORIGINS=*

# ==============================================================
# Rate limiting
# ==============================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ==============================================================
# Logging (error | warn | info | debug)
# ==============================================================
LOG_LEVEL=info

# ==============================================================
# Autenticación (se usará en una fase posterior — aún no implementado)
# ==============================================================
# JWT_SECRET=
# JWT_EXPIRES_IN=1d
